import { ImageNameToObjectURLPipe } from './image-name-to-object-url-pipe';

describe('ImageNameToObjectURLPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageNameToObjectURLPipe();
    expect(pipe).toBeTruthy();
  });
});
