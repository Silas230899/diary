import { FormatWrittenDatePipe } from './format-written-date-pipe';

describe('FormatWrittenDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatWrittenDatePipe();
    expect(pipe).toBeTruthy();
  });
});
